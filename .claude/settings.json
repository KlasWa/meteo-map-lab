{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c 'import sys,json;print(json.load(sys.stdin).get(\"tool_input\",{}).get(\"file_path\",\"\"))' | { read -r f; case \"$f\" in */frontend/*) (cd frontend && node_modules/.bin/prettier --ignore-unknown --write \"$f\");; esac; } 2>/dev/null || true",
            "statusMessage": "Prettier (frontend)"
          }
        ]
      }
    ]
  }
}
