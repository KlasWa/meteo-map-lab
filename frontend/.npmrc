# openapi-typescript@7 declares a typescript@^5 peer dep, but the Vite 8
# template ships TypeScript 6 (works fine at runtime). Relax peer-dep
# resolution so local, Docker, and dev-container installs all behave the same.
legacy-peer-deps=true
